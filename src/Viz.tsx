import React, { FunctionComponent } from 'react';
import { RouteComponentProps } from '@reach/router';

const Viz: FunctionComponent<RouteComponentProps> = () => {
  return <section className="home">Viz view</section>;
};

export default Viz;
