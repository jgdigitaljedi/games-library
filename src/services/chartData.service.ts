import { IGame } from '../common.model';

export default {
  makeDataSet: (data: IGame[], which: string): any[] => {
    console.log('data', data);
    console.log('which', which);
    return [];
  }
};
